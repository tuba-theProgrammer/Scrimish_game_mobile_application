<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.assignment2_tuba_asif">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ASSIGNMENT2_TUBA_ASIF">
        <activity android:name=".playerSetDeckActivity"></activity>
        <activity android:name=".GamePlayWindow" />
        <activity android:name=".previousScore_activity" />
        <activity android:name=".level_selection_activity" />
        <activity android:name=".CreatePiles_activity" />
        <activity android:name=".RegisterAcc_activity" />
        <activity android:name=".Login_activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>